/* exported isLowerCased */
/*
make a storage variable containing parameter with lower case method.
return parameter equals storage variable.
*/
function isLowerCased(word) {
  var lowerCased = word.toLowerCase();
  return word === lowerCased;
}
