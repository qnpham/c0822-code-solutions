/* exported isUpperCased */
/*
make a storage variable containing the parameter with upper case method.
return parameter equals storage variable
*/
function isUpperCased(word) {
  var upperCase = word.toUpperCase();
  return word === upperCase;
}
