/* exported setValue */
/*
set the key parameter to be a property key of object parameter using bracket notation
assign all of that to value parameter
*/
function setValue(object, key, value) {
  object[key] = value;
}
