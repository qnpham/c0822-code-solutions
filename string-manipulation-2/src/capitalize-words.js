/* exported capitalizeWords */
/*
make an empty result string
assign an array to parameter using split method with a space in string as argument.
make a loop starting from zero, as long as it is less than or equal to array length
minus one, it increments by one.
inside loop code block, make an if statement. If loop variable is equal to zero,
then concatenate result with parameter at loop index while using uppercase method.
else,
concatenate parameter at loop index while using lowercase method.
*/
